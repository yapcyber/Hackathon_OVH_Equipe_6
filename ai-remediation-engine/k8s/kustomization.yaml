apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - webhook-rbac.yaml
  - job-rbac.yaml
  - webhook-deployment.yaml
  - servicemonitor.yaml
# secrets.example.yaml volontairement exclu : les vrais secrets ne sont
# jamais gérés via ce Kustomization (cf. README ai-remediation-engine/).
